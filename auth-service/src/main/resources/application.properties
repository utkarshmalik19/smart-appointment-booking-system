spring.application.name=auth-service

server.port=8090

spring.datasource.url=jdbc:mysql://localhost:3306/auth_db
spring.datasource.username=root
spring.datasource.password=utkarsh
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JWT secret (dev only). Use a long random value.
jwt.expiration-in-ms=900000    # 15 minutes
jwt.refresh-expiration-ms=604800000  # 7 days

#Security
jwt.secretKey=ahwi9248fnxsok1ah29sksq012ndbn38skqbtue493ncl4s;

# Eureka configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.hostname=localhost
eureka.instance.prefer-ip-address=true
eureka.instance.non-secure-port=${server.port}